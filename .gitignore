# Project context docs — keep local, never commit
competitioncontext.md
idea.md
implementation.md

# Agent tooling & installed skills (third-party, reinstalled via skills-lock.json)
/.agents/
/.claude/
/.zcode/
skills-lock.json
.monskills
/docs/superpowers/
/web/AGENTS.md
/web/CLAUDE.md

# One-off funding helper (hardcoded WSL keystore path — not portable, no secrets)
/scripts/

# Secrets — never commit
.env
.env.local
.env.*.local
*.pem
*.key
monskills-keystore/
keystore/

# Node / Next.js (for when we scaffold web/)
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Foundry build artifacts (scoped to contracts/ — bare 'lib/' would also match web/lib/)
/contracts/cache/
/contracts/out/
/contracts/broadcast/
/contracts/lib/

# OS junk
.DS_Store
Thumbs.db